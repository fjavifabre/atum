# Add each test here
add_executable(firstTest sphere.cpp)

# Standard linking to gtest stuff.
target_link_libraries(firstTest gtest gtest_main)

# Standard linking to gtest stuff.
target_link_libraries(firstTest atum)
